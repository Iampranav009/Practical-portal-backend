# Railway deployment configuration using Nixpacks
[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm ci --only=production"]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "npm start"
